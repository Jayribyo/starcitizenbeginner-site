---
const referralCode = "STAR-FSRJ-5N7Z";
const referralLink = "https://www.robertsspaceindustries.com/enlist?referral=STAR-FSRJ-5N7Z";

const { title, blurb, ctaPrimary, ctaSecondary, ctaSecondaryHref = "#quickstart" } = Astro.props;
---
<div class="card" data-scb-component="referral-cta">
  <div class="kicker">Referral bonus</div>
  <h2 style="margin:10px 0 8px; font-size: 20px;">{title}</h2>
  <p style="margin:0 0 14px; color: rgba(255,255,255,.78);">{blurb}</p>

  <div style="display:flex; flex-wrap:wrap; gap:10px; align-items:center;">
    <span class="badge">
      Code: <strong style="color:#fff;">{referralCode}</strong>
    </span>
    <button class="btn ghost js-copyCode" type="button" data-copy={referralCode}>Copy code</button>
  </div>

  <div style="display:flex; flex-wrap:wrap; gap:10px; margin-top: 14px;">
    <a class="btn primary" data-scb-role="referral-cta" href={referralLink} rel="nofollow noopener" target="_blank">{ctaPrimary}</a>
    <a class="btn ghost" href={ctaSecondaryHref}>{ctaSecondary}</a>
  </div>
</div>
