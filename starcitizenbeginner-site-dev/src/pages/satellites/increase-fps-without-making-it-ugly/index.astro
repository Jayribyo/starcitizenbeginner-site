---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import ReferralBanner from "../../../components/ReferralBanner.astro";
import OnThisPage from "../../../components/OnThisPage.astro";
import FaqSchema from "../../../components/FaqSchema.astro";
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import { REFERRAL_CODE, REFERRAL_LINK, REFERRAL_INFO_PATH, REFERRAL_BONUS_UEC, FLY_FREE_URL } from "../../../config/site";

const referralCode = REFERRAL_CODE;
const referralLink = REFERRAL_LINK;

const heroImg = "/assets/img/star-citizen-performance-guide/star-citizen-best-settings-performance-optimization-hero.webp";

const tocItems = [
  { id: "quick-answer", label: "Quick answer" },
  { id: "do-this-now", label: "Do this now" },
  { id: "common-mistakes", label: "Common mistakes" },
  { id: "next-steps", label: "Next steps" },
  { id: "faq", label: "FAQ" }
];

const faqItems = [
  { q: "What’s the best ‘low ugliness’ lever?", a: "Shadows and volumetrics are common wins." },
  { q: "Is upscaling worth it?", a: "Often yes — it’s one of the cleanest FPS trades." },
  { q: "Should I disable all post-processing?", a: "Only if needed; start with the biggest levers first." },
  { q: "Why is one city always worse?", a: "Some scenes are heavier; use them to test worst-case." },
  { q: "Where do I go for a full baseline?", a: "The performance guide has a full checklist and requirements tiers." }
];
---

<BaseLayout
  title="How to Increase FPS in Star Citizen Without Making It Ugly | Star Citizen Beginner"
  description="How to increase FPS in Star Citizen without making it look bad: the few settings that usually help most, plus what is rarely worth touching."
  canonicalPath="/satellites/increase-fps-without-making-it-ugly/"
  ogImage={heroImg}
  ogImageAlt="Increase FPS Without Making It Ugly (High-Impact Tweaks)"
  stickyCtaLabel="Create account + claim bonus"
  stickyCtaHref={referralLink}
  stickyCtaExternal={true}
  activeNav="guides"
>
  <main>

    <Breadcrumbs items={[{ label: 'Home', href: '/' }, { label: 'Answers', href: '/satellites/' }, { label: 'Answer', href: '/satellites/increase-fps-without-making-it-ugly/' }]} />
    <section class="hero guideHero">
      <img
        class="hero__img"
        src={heroImg}
        alt="Increase FPS Without Making It Ugly (High-Impact Tweaks)"
        loading="eager"
        fetchpriority="high"
        decoding="async"
        width="1536"
        height="1024"
      />
      <div class="hero__overlay"></div>

      <div class="hero__content">
        <div class="guideHero__content">
          <div class="kicker"><span class="dot"></span> Starter Guide</div>
          <h1 class="h1">How to Increase FPS in Star Citizen Without Making It Ugly</h1>
          <p class="lead" style="max-width: 820px;">Answer: This page focuses on the settings that usually buy back FPS without making the game look flat or blurry, plus a simple way to test each change.</p>

          <div class="hero__cta">
            <a class="btn primary" href="#quick-answer">Start here</a>
          </div>
        </div>

        <OnThisPage variant="hero" title="On this page" items={tocItems} />
      </div>
    </section>

    <ReferralBanner
      code={referralCode}
      bonus="50,000"
      ctaLabel="Claim your start bonus"
      ctaHref={referralLink}
      ctaExternal={true}
    />

    <section id="quick-answer" class="section">
      <div class="container">
        <h2 class="h2">Quick answer</h2>

        <div class="card pad" style="margin-bottom:12px;">
          <div class="kicker"><span class="dot"></span>Answer</div>
          <p class="lead" style="margin-top:10px;">The trick is to cut expensive effects first, not overall detail everywhere.</p>
        </div>

        <ul class="bullets">
          <li>Small changes you can actually feel are better than chasing settings screenshots.</li>
          <li>The trick is to cut <b>expensive effects</b> first, not overall detail everywhere.</li>
<li>You can often keep readability and style by lowering a few heavy settings and using upscaling.</li>
<li>Verify changes in the same scene so you don’t chase placebo.</li>
        </ul>
      </div>
    </section>

    <section id="do-this-now" class="section alt">
      <div class="container">
        <h2 class="h2">Do this now</h2>
        <ol class="steps">
          <li><b>Lower volumetrics/clouds</b> before anything else.</li>
<li><b>Reduce shadow distance/quality</b> (big cost, moderate visual hit).</li>
<li><b>Use upscaling</b> to trade pixels for FPS while keeping UI readable.</li>
<li><b>Keep textures as high as VRAM allows</b> (textures are often cheaper than heavy effects).</li>
<li><b>Retest</b> in the same location; stop when the game feels smooth enough.</li>
        </ol>
      </div>
    </section>

    <section id="common-mistakes" class="section">
      <div class="container">
        <h2 class="h2">Common mistakes (and exact fixes)</h2>
        <div class="tableWrap">
          <table class="table">
            <thead>
              <tr>
                <th>Mistake</th>
                <th>What happens</th>
                <th>Fix (exact)</th>
              </tr>
            </thead>
            <tbody>
              <tr><td><b>Lowering textures first on a VRAM-rich GPU</b></td><td>You lose quality for little gain.</td><td>Target effects (clouds/shadows) first.</td></tr>
<tr><td><b>Chasing max FPS everywhere</b></td><td>You make the game ugly and still stutter in hubs.</td><td>Aim for ‘smooth enough’ + stable feel.</td></tr>
<tr><td><b>Ignoring CPU limits</b></td><td>GPU tweaks won’t fix CPU-bound scenes.</td><td>Run the CPU vs GPU bottleneck test.</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </section>

    <section id="next-steps" class="section alt">
      <div class="container">
        <h2 class="h2">Next steps</h2>
        <ul class="bullets">
          <li><a href="/star-citizen-performance-guide/">Open the full performance guide</a></li>
<li><a href="/satellites/best-settings-for-low-end-pc-star-citizen/">Weak system? Start with the low-end baseline instead</a></li>
<li><a href="/satellites/fix-stuttering/">If the problem is hitching, use the stutter checklist</a></li>
        </ul>
      </div>
    </section>

    <section id="faq" class="section">
      <div class="container">
        <h2 class="h2">FAQ</h2>
        <div class="faq" style="margin-top:12px;">
          {faqItems.map((item) => (
            <details class="faq__item">
              <summary class="faq__q">{item.q}</summary>
              <div class="faq__a">{item.a}</div>
            </details>
          ))}
        </div>
        
      </div>

      <FaqSchema items={faqItems} />
    </section>
  </main>
</BaseLayout>
